# lib

Thư mục này dùng để chứa các file thư viện JAR cần thiết cho project (ví dụ: MySQL Connector, thư viện ngoài,...).

Nếu không có file nào, git sẽ không lưu folder này. Hãy để lại file này để giữ thư mục trên GitHub.
